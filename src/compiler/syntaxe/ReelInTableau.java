package compiler.syntaxe;

import compiler.lexical.Tokens;

public class ReelInTableau extends Keyword{

	public ReelInTableau(int start) {
		super(start);
		if(currentToken.getId() == Tokens.INTEGER)
		{
			
		}
	}

}
